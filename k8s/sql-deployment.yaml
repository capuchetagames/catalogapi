apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-db
  template:
    metadata:
      labels:
        app: catalog-db
    spec:
      containers:
        - name: catalog-db
          image: mcr.microsoft.com/mssql/server:2022-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 1436
          env:
            
            - name: ACCEPT_EULA
              valueFrom:
                configMapKeyRef:
                  name: catalog-config
                  key: ACCEPT_EULA
            
            - name: SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: DB_PASSWORD