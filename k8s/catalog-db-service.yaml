apiVersion: v1
kind: Service
metadata:
  name: catalog-db
  namespace: fiapstore
  labels:
    app: catalog-db
spec:
  type: ClusterIP
  selector:
    app: catalog-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
