apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: fiapstore
  labels:
    app: catalog-api
spec:
  type: LoadBalancer
  selector:
    app: catalog-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
