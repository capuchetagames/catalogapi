apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: fiapstore
  labels:
    app: catalog-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      initContainers:
        - name: wait-for-catalog-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for catalog-db:5432..."
              until nc -z catalog-db 5432; do
                echo "catalog-db not ready yet, sleeping 3s..."
                sleep 3
              done
              echo "catalog-db is up."
      containers:
        - name: catalog-api
          image: DOCKERHUB_USER/catalog-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: catalog-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: ConnectionStrings__DefaultConnection
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: Jwt__Key
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
