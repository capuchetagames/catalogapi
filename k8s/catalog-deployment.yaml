apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: fiapstore
  labels:
    app: catalog-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      containers:
        - name: catalog-api
          image: DOCKERHUB_USER/catalog-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: catalog-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: ConnectionStrings__DefaultConnection
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: Jwt__Key
            - name: ElasticSettings__ApiKey
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: ElasticSettings__ApiKey
            - name: ElasticSettings__CloudId
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: ElasticSettings__CloudId
            - name: ElasticSettings__UseCloud
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: ElasticSettings__UseCloud
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: AWS_ACCESS_KEY_ID
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: AWS_SECRET_ACCESS_KEY
            - name: AWS_DEFAULT_REGION
              valueFrom:
                secretKeyRef:
                  name: catalog-secret
                  key: AWS_DEFAULT_REGION
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
